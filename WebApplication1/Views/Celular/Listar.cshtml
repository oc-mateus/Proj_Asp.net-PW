@model IEnumerable<WebApplication1.Models.Celular>

@{
    ViewBag.Title = "Lista de Celulares";
}

<h2 class="mb-4">Lista de Celulares</h2>

<div class="mb-3">
    @Html.ActionLink("Cadastrar", "Create", null, new { @class = "btn btn-outline-primary" })
    <a href="@Url.Action("DownloadPdf", "Pdf", new { tema = "celular" })" class="btn btn-outline-danger">📄 Exportar PDF</a>
    <a href="@Url.Action("DownloadExcel", "Excel", new { tema = "celular" })" class="btn btn-outline-success">📊 Exportar Excel</a>
</div>

<table id="tabelaCelulares" class="table table-bordered table-hover">
    <thead class="thead-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Numero)</th>
            <th>@Html.DisplayNameFor(model => model.Marca)</th>
            <th>@Html.DisplayNameFor(model => model.Fabrica)</th>
            <th>@Html.DisplayNameFor(model => model.Novo)</th>
            <th style="width: 150px;">Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var celular in Model)
        {
            <tr>
                <td>@celular.Numero</td>
                <td>@celular.Marca</td>
                <td>@celular.GetDataCelular()</td>
                <td>@(celular.Novo ? "Sim" : "Não")</td>
                <td>
                    @Html.ActionLink("✏️ Editar", "Edit", new { id = celular.Id }, new { @class = "btn btn-outline-info btn-sm" })
                    <button class="btn btn-outline-danger btn-sm" onclick="confirmarExclusao(@celular.Id, '@celular.Marca')">🗑 Excluir</button>
                </td>
            </tr>
        }
    </tbody>
</table>

<!-- Modal de Confirmação -->
<div class="modal fade" id="modalExcluir" tabindex="-1" role="dialog" aria-labelledby="modalExcluirLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="modalExcluirLabel">Confirmar Exclusão</h5>
                <button type="button" class="close text-white" data-dismiss="modal" aria-label="Fechar">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Tem certeza que deseja excluir o celular <strong id="celularNome"></strong>?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                <button type="button" class="btn btn-danger" id="btnConfirmarExcluir">Excluir</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.datatables.net/1.10.25/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.25/js/dataTables.bootstrap4.min.js"></script>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.10.25/css/dataTables.bootstrap4.min.css" />

    <script>
        var idCelularParaExcluir = 0;

        function confirmarExclusao(id, nome) {
            idCelularParaExcluir = id;
            $("#celularNome").text(nome);
            $("#modalExcluir").modal('show');
        }

        $("#btnConfirmarExcluir").click(function () {
            $.post('@Url.Action("DeleteAjax")', { id: idCelularParaExcluir }, function (data) {
                if (data.sucesso) {
                    $("#modalExcluir").modal('hide');
                    location.reload();
                } else {
                    alert("Erro ao excluir celular: " + data.mensagem);
                }
            }).fail(function () {
                alert("Erro ao se comunicar com o servidor.");
            });
        });

        $(document).ready(function () {
            $('#tabelaCelulares').DataTable({
                language: {
                    "search": "Buscar:",
                    "lengthMenu": "Mostrar _MENU_ registros",
                    "info": "Mostrando _START_ a _END_ de _TOTAL_ celulares",
                    "paginate": {
                        "first": "Primeiro",
                        "last": "Último",
                        "next": "Próximo",
                        "previous": "Anterior"
                    }
                }
            });
        });
    </script>
}
